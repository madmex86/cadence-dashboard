name: Keep Supabase Alive

on:
  schedule:
    # Runs at 00:00 UTC every 3 days
    - cron: '0 0 */3 * *'
  workflow_dispatch: # Allows manual triggering

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase Database
        run: |
          curl -sf -X GET "https://ufqiysdgmxrhonnfsgts.supabase.co/rest/v1/creatures?select=id&limit=1" \
          -H "apikey: sb_publishable_7mkBL1lsKUNJEmqSd2HT9Q_Z4xHoBec" \
          -H "Authorization: Bearer sb_publishable_7mkBL1lsKUNJEmqSd2HT9Q_Z4xHoBec"
